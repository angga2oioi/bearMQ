{
  "name": "bearMQ",
  "version": "1.0.0",
  "scripts": {
    "build": "node esbuild.config.js",
    "start:production": "pm2 start processes.json --no-daemon",
    "start:local": "node ./cmd/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "esbuild": "^0.25.4",
    "ws": "^8.18.2"
  }
}
